"""sih-shared: protocol, crypto, credential-state, framing and TLS primitives.

Everything here is pure logic with no I/O except for the ``tls`` and
``framing`` helpers which are explicitly socket/certificate oriented.
"""
